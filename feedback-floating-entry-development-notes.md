# 悬浮意见反馈入口｜前端开发说明

配套原型：`feedback-floating-entry-demo.html`

## 1. 功能边界

- 当前入口只进入现有“意见反馈”流程页面，不展示 AI 问答能力。
- 悬浮入口与右上角入口指向同一反馈页面。
- 右上角入口始终存在，是悬浮入口隐藏或部分应用无法展示时的兜底。
- 悬浮入口属于全局能力，建议由应用壳层统一挂载，不要在每个业务页面重复实现。

## 2. 建议拆分

| 组件 | 职责 |
|---|---|
| `GlobalFeedbackOrb` | 悬浮球展示、拖拽、吸边、展开标签、收起动效 |
| `HeaderFeedbackEntry` | 顶栏固定入口、隐藏状态标记、恢复入口菜单 |
| `FeedbackEntrySettings` | 国小君首页中的显示开关 |
| `FeedbackEntryService` | 读取和保存用户配置、拼装反馈页来源参数 |

如果各应用不是同一个前端工程，建议封装成统一组件包或微前端公共能力，避免不同应用的尺寸、动效和状态逻辑不一致。

## 3. 状态模型

建议至少保存以下状态：

```ts
type FeedbackEntryPreference = {
  enabled: boolean; // 是否显示悬浮入口
  positionMode: 'default' | 'custom';
  side: 'left' | 'right';
  topRatio?: number; // 自定义位置建议存视口高度比例，不直接存固定像素
  firstGuideShown: boolean;
};
```

- `enabled`：用户级全局配置，收起后设为 `false`。
- `positionMode`：未拖拽时为 `default`；用户完成拖拽后设为 `custom`。
- `side + topRatio`：记录吸附边和纵向位置。
- `firstGuideShown`：控制“意见反馈”首次展开提示只出现一次。

原型使用 `localStorage` 模拟。生产环境中，`enabled` 建议保存到国小君用户配置服务，以便跨应用、跨设备一致；拖拽位置可以先保存在本机。

## 4. 页面初始化

```text
应用壳层加载
  → 读取用户配置
  → enabled = true：显示悬浮球并恢复位置
  → enabled = false：隐藏悬浮球，顶栏入口显示隐藏状态标记
```

默认位置规则：

- 左侧菜单展开：距视口左侧约 `20px`，距底部约 `150px`。
- 左侧菜单收起：移动到折叠菜单右侧，约 `菜单宽度 + 18px`。
- 如果用户拖拽过，尊重自定义位置，不再跟随菜单移动。
- 页面尺寸变化时重新计算边界，不能让入口移出可视区。

## 5. 点击和跳转

悬浮球主体及右上角入口点击后，新标签页打开意见反馈流程，避免丢失当前页面未保存的内容。

建议传递：

```ts
type FeedbackSourceContext = {
  terminal: 'web';
  appId: string;
  moduleId?: string;
  pageName: string;
  pageUrl: string;
};
```

反馈页根据来源信息自动带出“终端”和“应用模块”。不要自动携带页面表单内容、员工明细等敏感数据。

## 6. 拖拽与吸边

1. 指针按下后移动超过 `6px`，才判定为拖拽；否则判定为点击。
2. 拖拽时收回文字标签，只保留圆球。
3. 可移动范围：顶栏下方至页面底部安全区，左右保留约 `18px`。
4. 松开后吸附到最近的左侧或右侧。
5. 吸边完成后保存 `side` 和 `topRatio`，并将位置模式改为 `custom`。
6. 停在左侧时标签向右展开；停在右侧时标签向左展开。

触摸设备使用 Pointer Events，并设置 `touch-action: none`，避免拖动时触发页面滚动。

## 7. 收起与恢复

收起流程：

```text
点击“收起悬浮入口”
  → 悬浮球缩小并飞向右上角入口
  → 动画结束后 enabled = false
  → 顶栏入口闪烁 2 次
  → 显示收起提示 8 秒
```

提示文案：

> 悬浮入口已收起，可从这里进入  
> 如需重新显示，可在「国小君首页」进行配置。

恢复入口有两处：

- 提示中的“前往配置”。
- 提示消失后，点击右上角入口，在菜单中选择“显示悬浮入口”。

因此，即使用户没有及时点击提示，也不会出现无法恢复的问题。

## 8. 顶栏入口逻辑

- 悬浮球显示时：点击顶栏图标直接进入意见反馈。
- 悬浮球隐藏时：点击顶栏图标先打开菜单：
  - 进入意见反馈
  - 显示悬浮入口
- 隐藏状态可以增加小蓝点，但不能表现为未读消息红点。
- 闪烁只在刚收起时播放，不持续闪烁。

## 9. 左侧菜单联动

```text
菜单展开/收起
  → positionMode = default：悬浮球平滑移动到新的默认位置
  → positionMode = custom：悬浮球保持用户位置，只做越界修正
```

这样默认状态不会遮挡收起后的窄菜单，同时不会覆盖用户已经做出的拖拽选择。

## 10. 动效参数

| 动效 | 建议时长 |
|---|---:|
| 悬停展开标签 | 160～200ms |
| 松手吸边 | 180～220ms |
| 飞向顶栏 | 700～900ms |
| 顶栏闪烁 | 2 次，每次约 540ms |
| 收起提示 | 8 秒后自动关闭 |

需要识别 `prefers-reduced-motion`。用户开启“减少动态效果”时，将飞行动效替换为快速淡出/淡入。

## 11. 层级与可访问性

- 悬浮入口层级应高于业务内容、低于全屏弹窗和确认对话框。
- 按钮必须提供 `aria-label`、键盘焦点样式和可点击区域。
- 悬浮球不要遮挡分页、表单提交、右侧操作栏等关键控件。
- 拖拽不是唯一操作方式；点击、收起和恢复都必须能通过键盘完成。

## 12. 验收清单

- [ ] 所有业务页面都有顶栏固定入口。
- [ ] 配置开启时，各业务页面显示悬浮入口。
- [ ] 点击与拖拽不会互相误触。
- [ ] 左右吸边、窗口缩放和菜单收起后均不越界。
- [ ] 用户拖拽过后，菜单展开/收起不擅自重置位置。
- [ ] 收起动画结束后再保存隐藏状态。
- [ ] 刷新后隐藏状态符合用户配置。
- [ ] 提示消失后仍可通过顶栏入口恢复。
- [ ] 反馈页面能正确获得来源应用和模块信息。
- [ ] 当前版本不出现 AI 问答入口或相关承诺文案。

## 13. 原型代码对应关系

- `positionOrb()`：计算默认位置、恢复位置和窗口边界。
- `pointerdown / pointermove / pointerup`：拖拽阈值、移动和吸边。
- `animateHide()`：悬浮球飞向顶栏并进入隐藏状态。
- `animateRestore()`：从顶栏恢复悬浮入口。
- `setHidden()`：同步悬浮球、顶栏标记和配置开关状态。
- `setMenuCollapsed()`：左侧菜单状态及默认悬浮位置联动。
- `openFeedback()`：打开意见反馈新页面。

原型中的业务页面、上传和提交均为演示数据，开发时应接入现有意见反馈流程和统一用户配置能力。
